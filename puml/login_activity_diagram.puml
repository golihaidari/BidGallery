@startuml
title Login Use Case Activity Diagram

start

:Customer navigates to Login page;
:Enter email/password OR choose "Login with Google";

if (Google OAuth?) then (yes)
  :Redirect to Google/Firebase login;
  :Receive OAuth token;
else (no)
  :Send email/password to backend;
  :Backend validates credentials;
endif

if (Authentication successful?) then (yes)
  :Generate JWT for session;
  :Store JWT in AuthContext;
  :Redirect to Product Page ;
else (no)
  :Show error message "Invalid credentials";
  :Allow retry;
endif

stop
@enduml
